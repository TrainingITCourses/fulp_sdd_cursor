# Normalize all to LF
* text=auto eol=lf

# Fils that must keep CRLF (native Windows scripts)
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Binaries: do not touch
*.png binary
*.jpg binary
*.woff2 binary